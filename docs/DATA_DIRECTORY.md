# Data directory

Artemis Desktop stores user state in `~/.artemis` on Linux/macOS
(`%LOCALAPPDATA%\artemis` on Windows).

## Environment

- `ARTEMIS_HOME` selects the home directory (preferred).
- When unset, the default is `~/.artemis`.
